# 蓝桥杯单片机备赛工程（第15届 / 第16届）

<p align="center">
  <img alt="MCU" src="https://img.shields.io/badge/MCU-STC15F2K61S2-1f6feb">
  <img alt="Language" src="https://img.shields.io/badge/Language-C-2ea44f">
  <img alt="Status" src="https://img.shields.io/badge/Status-Training-orange">
  <img alt="Contest" src="https://img.shields.io/badge/Contest-蓝桥杯省赛15%2F16届-blueviolet">
</p>

> 这是我的蓝桥杯单片机备赛工程仓库，覆盖第15届与第16届真题，用于题目复现、模块联调、评测纠错与最终冲刺。

---

## 1. 项目目标

- 快速复现历届真题核心功能
- 搭建稳定的模块化驱动框架
- 形成可复用的评测排错流程
- 在赛前完成高频错误归档与修正

---

## 2. 当前工程概览

### 2.1 主控与平台

- 芯片: STC15F2K61S2
- 开发方向: 蓝桥杯单片机省赛
- 工程类型: C + Keil 工程

当前训练主线:

- 主联调工程: `16届蓝桥杯真题/`
- 对照复盘工程: `15届蓝桥杯真题/`

### 2.2 功能模块

- 温度采集: DS18B20
- 超声波测距: superwave
- 光敏采样: AD/DA
- 数码管显示: digital_tube
- 按键输入: kbd / kdb_nb
- 串口调试: uart
- 基础外设: delay / init / iic / onewire

### 2.3 多工程结构说明（已按仓库实际补全）

- `15届蓝桥杯真题/`: 第十五届题目复现与联调工程（独立 Keil 工程）
- `16届蓝桥杯真题/`: 第十六届主备赛工程（当前 README 重点描述对象）
- `测试/`: 最小化测试工程（用于快速验证单点修改）
- `练习题/LED/`: 基础 LED 练习工程
- `练习题/通过串口接收到的数据控制ALED1的状态/`: 串口输入控制 LED 的专项练习
- `STC15.h`: 芯片寄存器头文件（根目录公共文件）

---

## 3. 目录结构（核心）

```text
16届蓝桥杯真题/
├─ main.c                 # 主流程与状态机
├─ superwave.c            # 超声波测距
├─ DS18B20.c              # 温度采集
├─ AD_DA.c                # AD/DA 读写
├─ digital_tube.c         # 数码管显示
├─ kbd.c / kdb_nb.c       # 按键处理
├─ init.c                 # 上电初始化
├─ uart.c                 # 串口模块
├─ one_wire.c / onewire.c # 单总线实现（历史版本）
├─ project.uvproj         # Keil 工程文件
├─ Listings/              # 编译列表文件
└─ Objects/               # 编译产物
```

补充：仓库中还存在多个可独立打开的 Keil 工程，建议按训练目标切换，不要在同一工程里混改。

```text
15届蓝桥杯真题/project.uvproj
16届蓝桥杯真题/project.uvproj
测试/project.uvproj
练习题/LED/project.uvproj
练习题/通过串口接收到的数据控制ALED1的状态/project.uvproj
```

---

## 4. 构建与下载

1. 使用 Keil 打开目标工程的 `project.uvproj`
2. 确认目标芯片与时钟配置
3. 全编译并检查 `Objects/` 产物
4. 通过 STC-ISP 下载到开发板

常用入口建议:

- 练真题主线: `16届蓝桥杯真题/project.uvproj`
- 对照题型与模块迁移: `15届蓝桥杯真题/project.uvproj`
- 验证一个改动是否引入副作用: `测试/project.uvproj`
- 基础巩固: `练习题/LED/project.uvproj`
- 串口专项: `练习题/通过串口接收到的数据控制ALED1的状态/project.uvproj`

建议:

- 每次改动后先做单模块验证，再做整题联调
- 保留一份“可过评测”的稳定版本标签

---

## 5. 训练节奏建议

- 每日 1 题模块复盘（20-40 分钟）
- 每日 1 次完整流程仿真（含边界条件）
- 每周 1 次全真评测回归
- 每周更新一次错题与坑点记录

---

## 6. 评测排错清单

- 继电器位定义是否与板卡映射一致
- 超声波换算系数和超时逻辑是否统一
- 运动状态判定周期与采样周期是否匹配
- 参数页面锁定逻辑是否符合题面
- 统计显示是否要求前导 0
- 阈值判断是 `<` 还是 `<=` 是否一致

---

## 7. 版本记录模板

### YYYY-MM-DD

- 调整模块:
- 变更说明:
- 评测结果:
- 遗留问题:
- 下一步计划:

---

## 8. 常见错误档案（第十六届省赛复盘）

> 以下错误均在第十六届省赛真题评测中实际踩到，按"现象 → 根因代码位置 → 修正方向"归档。

---

### 8.1 继电器吸合/断开方向相反

| 项目 | 内容 |
|------|------|
| **现象** | 应吸合却断开，应断开却吸合，在相邻测试步骤出现互换 |
| **根因** | `RELAY_BIT` 位掩码与板卡实际继电器位不一致 |
| **代码位置** | `main.c` — `#define RELAY_BIT` |
| **修正方向** | 以官方资源包行为为唯一基准：`Uln_Ctrl(0x10)` 即吸合继电器，`RELAY_BIT = 0x10` |
| **坑点** | OCR 读原理图时可能与实际字节顺序不符，优先看官方参考实现，不要只看原理图网名 |

---

### 8.2 参数界面调参时继电器/LED 状态丢失

| 项目 | 内容 |
|------|------|
| **现象** | 进入参数界面调参过程中，继电器意外断开；L8 该闪烁的变成常亮或熄灭 |
| **根因** | `output_lock` 分支直接冻结了全部 LED，L8 未继续执行闪烁逻辑 |
| **代码位置** | `main.c` — `void output_update()` → `if(output_lock)` 分支 |
| **修正方向** | 锁定时将 L1–L7 冻结，L8 仍按进入参数页时的 `move_state` 继续 STOP/WALK/RUN 逻辑 |

---

### 8.3 统计界面 NC 显示前导零格式错误

| 项目 | 内容 |
|------|------|
| **现象** | 期望显示 `NC 3`，实际显示 `NC 0003` |
| **根因** | `stat_display()` 固定输出 4 位数，高位补零 |
| **代码位置** | `main.c` — `void stat_display()` |
| **修正方向** | 当 `n < 1000/100/10` 时，对应位熄灭（`digital_tube(pos, 0xFF)`），不补 0 |
| **坑点** | 数码管段码 `0xFF` 表示全段熄灭，可用于"不显示某位" |

---

### 8.4 运动状态判断偏差（L 状态等级错误，距离显示偏移）

| 项目 | 内容 |
|------|------|
| **现象** | 应判 WALK 却显示 STOP；距离变化与状态切换对不上 |
| **根因一** | 距离采样周期与运动判定周期不统一，位移差取的是 200ms 差值，但阈值是按 1s 尺度设计的 |
| **根因二** | 状态锁定每次都触发，导致长时间无法更新状态 |
| **代码位置** | `main.c` — `void update_move_state()` / 主循环采样间隔 |
| **修正方向** | 确保 delta 比较使用同一时间窗；只在状态发生变化时才触发锁定 |

---

### 8.5 超声波距离偏差

| 项目 | 内容 |
|------|------|
| **现象** | 测距结果比期望值恒定偏大约 1.18%；远距离/弱回波时偶尔误返 0 |
| **根因一** | 换算系数 `0.0172` 与官方 `0.017` 不一致 |
| **根因二** | 回波超时用 `CH < 0x40` 而非 PCA 溢出标志 `CF`，导致远距离提前退出 |
| **代码位置** | `superwave.c` — 换算系数 / 回波等待条件 |
| **修正方向** | 系数改为 `0.017`；超时改为 `while(RX && !CF)` + `if(CF)` 判断 |

---

### 8.6 接近判定边界异常

| 项目 | 内容 |
|------|------|
| **现象** | 距离为 0 时不触发接近，某些场景错误触发 |
| **根因** | `distance > 0 && distance < dis_para` 的组合条件与评测基准不一致 |
| **代码位置** | `main.c` — `void judge_state()` |
| **修正方向** | 以官方参考行为为准，使用纯 `distance < dis_para` |

---

### 8.7 温度采样滞后一拍

| 项目 | 内容 |
|------|------|
| **现象** | 场景切换时温度判定比实际值落后一个周期，相邻测试步骤结果像"串了一步" |
| **根因** | `read_temp_data()` 里"先 convertT 再 readT"实际读的是上一次转换结果 |
| **代码位置** | `main.c` — `void read_temp_data()` |
| **修正方向** | 改为"先 readT，再 convertT"，每次调用都能拿到当前最新值 |

---

### 8.8 Git 仓库状态混乱导致版本错误

| 项目 | 内容 |
|------|------|
| **现象** | 提交列表全部变黄；以为在某版，实际不在该分支 |
| **根因** | `git switch --detach` 进入 detached HEAD 状态；另有未完成的 cherry-pick 残留 |
| **修正方向** | 回到指定版本时应 `git switch main` + `git reset --hard <commit>`，不在 detached HEAD 下直接改代码 |

---

## 9. 个人备赛宣言

稳住节奏，先保正确，再提速度。
每个错误都复盘成经验，每次评测都比上一次更接近满分。

---

## 10. 题目改动记录规范（重点）

为避免“改完能跑但偏离题面”，所有题目层面的改动都按以下模板记录：

### 10.1 单条记录模板

- 所属题目: 第15届 / 第16届
- 题面原要求:
- 当前改动内容:
- 改动原因: 评测不通过 / 与官方样例不一致 / 边界条件修正
- 影响模块: 例如 `main.c` / `kbd.c` / `superwave.c`
- 回归结果: 通过项 / 未通过项
- 结论: 保留 / 回退 / 继续调整

### 10.2 改动原则

- 先对齐题面，再做优化，不以“看起来更合理”替代题目要求。
- 涉及阈值、显示格式、时序窗口的改动必须做前后对比记录。
- 题目逻辑改动后，至少回归一次“测试工程 + 真题工程”。

---

## 11. 使用约定（仓库实践）

- 每次只改一个工程目录，改完先在该目录完成编译通过再切换。
- `Listings/` 与 `Objects/` 主要用于查看编译结果，不作为核心源码维护区。
- 同名/近名驱动（如 `onewire.c` 与 `one_wire.c`）共存时，以当前打开工程内实际参与编译的文件为准。
- 评测前优先用 `测试/` 做最小回归，再回到真题工程做全流程联调。

---

## 12. 真题状态速览

- 第15届: 已建立完整工程，包含频率测量、参数页、时间页与历史最高值回看链路。
- 第16届: 已建立完整工程，覆盖环境页/运动页/参数页/统计页与多外设联动。
- 建议节奏: 日常主练第16届，隔日用第15届做“跨题型迁移”回归。

---
